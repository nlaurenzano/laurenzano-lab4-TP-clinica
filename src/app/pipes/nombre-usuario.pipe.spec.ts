import { NombreUsuarioPipe } from './nombre-usuario.pipe';

describe('NombreUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
