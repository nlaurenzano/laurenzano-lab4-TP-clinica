import { MostrarIconoPipe } from './mostrar-icono.pipe';

describe('MostrarIconoPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarIconoPipe();
    expect(pipe).toBeTruthy();
  });
});
