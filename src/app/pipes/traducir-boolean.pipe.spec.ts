import { TraducirBooleanPipe } from './traducir-boolean.pipe';

describe('TraducirBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new TraducirBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
