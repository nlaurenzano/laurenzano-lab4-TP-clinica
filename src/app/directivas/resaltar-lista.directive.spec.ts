import { ResaltarListaDirective } from './resaltar-lista.directive';

describe('ResaltarListaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarListaDirective();
    expect(directive).toBeTruthy();
  });
});
