import { ResaltarTurnoDirective } from './resaltar-turno.directive';

describe('ResaltarTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
